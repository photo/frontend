[site]
lastCodeVersion="{lastCodeVersion}"

[paths]
adapters="{adapters}"
configs="{configs}"
controllers="{controllers}"
docroot="{docroot}"
external="{external}"
libraries="{libraries}"
models="{models}"
photos="{photos}"
plugins="{plugins}"
templates="{templates}"
themes="{themes}"
temp="{temp}"
userdata="{userdata}"

[plugins]
activePlugins=""

[systems]
database="{database}"
fileSystem="{fileSystem}"

[credentials]
awsKey="{awsKey}"
awsSecret="{awsSecret}"
dropboxKey="{dropboxKey}"
dropboxSecret="{dropboxSecret}"
dropboxToken="{dropboxToken}"
dropboxTokenSecret="{dropboxTokenSecret}"

[aws]
s3BucketName="{s3Bucket}"
s3Host="{s3Host}"
simpleDbDomain="{simpleDbDomain}"
dynamoDbPrefix="{dynamoDbPrefix}"

[dropbox]
dropboxFolder="{dropboxFolder}"

[mysql]
mySqlHost="{mySqlHost}"
mySqlUser="{mySqlUser}"
mySqlPassword="{mySqlPassword}"
mySqlDb="{mySqlDb}"
mySqlTablePrefix="{mySqlTablePrefix}"

[postgresql]
postgreSqlHost="{postgreSqlHost}"
postgreSqlUser="{postgreSqlUser}"
postgreSqlPassword="{postgreSqlPassword}"
postgreSqlDb="{postgreSqlDb}"
postgreSqlTablePrefix="{postgreSqlTablePrefix}"

[localfs]
fsRoot="{fsRoot}"
fsHost="{fsHost}"

[modules]
image="{imageLibrary}"
exiftran="{exiftran}"

[photos]
baseSize="1280"
autoTagWithDate={autoTagWithDate}

[secrets]
secret="{localSecret}"

[application]
appId="{appId}"

[theme]
name="{theme}"

[user]
email="{email}"
